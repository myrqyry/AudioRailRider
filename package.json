{
  "name": "audiorailrider-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"pnpm --filter ./frontend run dev\" \"cd backend && PYTHONPATH=..:$PYTHONPATH poetry run uvicorn main:app --reload --host 0.0.0.0 --port 8000\"",
    "setup": "pnpm install --recursive",
    "postinstall": "pnpm run backend:install",
    "frontend:dev": "pnpm --filter ./frontend run dev",
    "backend:install": "cd backend && poetry install",
    "backend:dev": "cd backend && poetry run uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "build": "pnpm --filter ./frontend run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "all": "^0.0.0"
  }
}
