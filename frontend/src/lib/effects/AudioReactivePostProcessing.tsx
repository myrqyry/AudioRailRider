// NOTE: Optional R3F-based effect wrapper is intentionally disabled for now.
// The active implementation is the imperative class in `AudioReactivePostProcessing.ts`.
// This stub is kept only to satisfy imports without pulling in R3F/postprocessing at runtime.

export function AudioReactivePostProcessing(): null {
  return null;
}
